{
  "rules": {
    "stations": {
      ".read": "auth != null",
      "$stationId": {
        ".write": "auth != null && (auth.token.admin === true || auth.token.stationId === $stationId)"
      }
    },
    "metadata": {
      ".read": "auth != null",
      ".write": false
    }
  }
}
